#include "main.h"

/**
 * mul - check if a character is a digit
 *
 * @x: the number to be checked
 *
 * @y: the number to be checked
 *
 * Return: return 0
 */

int mul(int x, int y)
{
	return (x * y);
}
